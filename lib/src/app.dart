import 'package:flutter/material.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(ctx) {
    return MaterialApp(
      home: Center(
        child: Text('Hello World!'),
      ),
    );
  }
}
